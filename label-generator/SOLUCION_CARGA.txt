🚨 PROBLEMA: Se queda cargando
==================================

✅ DIAGNÓSTICO:
- Servidor funcionando: ✓
- Supabase conectado: ✓  
- Tablas de base de datos: ❌ FALTA CREAR

🔧 SOLUCIÓN (2 MINUTOS):

1. Abrir en navegador: https://dblyptsepvhulpllqamz.supabase.co

2. Ir a "SQL Editor" (menú lateral izquierdo)

3. Copiar y pegar este SQL:

```sql
CREATE TABLE IF NOT EXISTS label_batches (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    type TEXT NOT NULL,
    quantity INTEGER NOT NULL,
    codes TEXT[] NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::TEXT, NOW()) NOT NULL,
    created_by TEXT,
    printed BOOLEAN DEFAULT FALSE NOT NULL
);

CREATE TABLE IF NOT EXISTS labels (
    id BIGSERIAL PRIMARY KEY,
    code TEXT NOT NULL UNIQUE,
    type TEXT NOT NULL,
    batch UUID NOT NULL REFERENCES label_batches(id) ON DELETE CASCADE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::TEXT, NOW()) NOT NULL,
    printed BOOLEAN DEFAULT FALSE NOT NULL,
    printed_at TIMESTAMP WITH TIME ZONE,
    printed_by TEXT
);

CREATE INDEX IF NOT EXISTS idx_labels_code ON labels(code);
CREATE INDEX IF NOT EXISTS idx_labels_type ON labels(type);
CREATE INDEX IF NOT EXISTS idx_labels_batch ON labels(batch);

ALTER TABLE label_batches ENABLE ROW LEVEL SECURITY;
ALTER TABLE labels ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow all operations on label_batches" ON label_batches
    FOR ALL USING (true) WITH CHECK (true);

CREATE POLICY "Allow all operations on labels" ON labels
    FOR ALL USING (true) WITH CHECK (true);
```

4. Presionar "RUN" o "Ejecutar"

5. Recargar http://localhost:3002

🎯 RESULTADO:
¡Sistema funcionando al 100%! Podrás generar etiquetas inmediatamente.

⚡ ALTERNATIVA RÁPIDA:
Si tienes problemas, simplemente copia el archivo database/schema.sql completo.